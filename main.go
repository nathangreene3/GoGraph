package graph

func main() {
	test0()
}

func test0() {
	// o := Origin(2)
	// g := NewGraph()
	// f := func(u *Vertex) bool {
	// 	return u.value.(Point).Distance(o) < 1.0
	// }
	// g.Insert(NewVertex(o), f, false)
	// for i := 0; i < 100; i++ {
	// 	g.Insert(NewVertex(Point{rand.Float64(), rand.Float64()}), f, false)
	// }
	// fmt.Printf("%s", g)
}
